import org.junit.jupiter.api.Test;

public class BurstDirectionTest {
    @Test
    public void testOutsideDirection() {
       /* Vector directionVector = BurstDirection.OUTSIDE.getDirectionVector(16, 0);
        System.out.println(directionVector.toString());
        directionVector = BurstDirection.OUTSIDE.getDirectionVector(16, 1);
        System.out.println(directionVector.toString());
        directionVector = BurstDirection.OUTSIDE.getDirectionVector(16, 2);
        System.out.println(directionVector.toString());
        directionVector = BurstDirection.OUTSIDE.getDirectionVector(16, 3);
        System.out.println(directionVector.toString());*/
    }
}
